<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<StackPane xmlns:fx="http://javafx.com/fxml"
           alignment="CENTER"
           fx:controller="tn.esprit.synergygig.controllers.ApplicationsAdminController">

    <!-- 🌌 Background animé -->
    <Pane fx:id="animatedBackground"/>

    <!-- Glass Container -->
    <VBox spacing="20"
          maxWidth="1000"
          prefWidth="1000"
          alignment="TOP_CENTER"
          style="-fx-padding:40;
                 -fx-background-color: rgba(255,255,255,0.05);
                 -fx-background-radius:20;
                 -fx-border-color: rgba(255,255,255,0.1);
                 -fx-border-radius:20;">

        <Label text="📬 Candidatures reçues"
               styleClass="title-galaxy"/>

        <TableView fx:id="applicationTable"
                   VBox.vgrow="ALWAYS"
                   styleClass="table-glass">

            <columns>
                <TableColumn fx:id="colOffer" text="Offre"/>
                <TableColumn fx:id="colApplicant" text="Candidat"/>
                <TableColumn fx:id="colStatus" text="Status"/>
                <TableColumn fx:id="colActions" text="Actions"/>
            </columns>

        </TableView>

    </VBox>

</StackPane>
