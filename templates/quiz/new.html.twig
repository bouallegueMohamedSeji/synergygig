{% extends 'base.html.twig' %}

{% block title %}Create New Quiz{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-md-6">
        <h1 class="h3 mb-4"><i class="bi bi-plus-circle"></i> Create New Quiz</h1>

        {% if errors %}
            <div class="alert alert-danger shadow-sm">
                <ul class="mb-0">
                    {% for error in errors %}
                        <li>{{ error }}</li>
                    {% endfor %}
                </ul>
            </div>
        {% endif %}

        <div class="card border-0 shadow-sm">
            <div class="card-body">
                <form method="post">
                    <div class="mb-3">
                        <label for="title" class="form-label">Quiz Title</label>
                        <input type="text" id="title" name="title" class="form-control" required placeholder="Enter quiz title..." value="{{ app.request.request.get('title') }}">
                    </div>

                    <div class="mb-4">
                        <label for="course" class="form-label">Associated Course</label>
                        <select id="course" name="course" class="form-select" required>
                            <option value="">Select a course...</option>
                            {% for course in courses %}
                                <option value="{{ course.id }}" {{ app.request.request.get('course') == course.id ? 'selected' : '' }}>
                                    {{ course.title }}
                                </option>
                            {% endfor %}
                        </select>
                    </div>

                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-primary d-flex align-items-center justify-content-center">
                            <i class="bi bi-check-circle me-2"></i> Create Quiz
                        </button>
                        <a href="{{ path('quiz_index') }}" class="btn btn-light">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
{% endblock %}
